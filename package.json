{
  "name": "eth-erc1155",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "cp dist/src/config/config.json src/config/config.json 2>/dev/null || : && cp dist/src/config/abi.json src/config/abi.json 2>/dev/null || :",
    "build": "npm run prebuild && tsc",
    "build:watch": "tsc --watch",
    "compile": "npx hardhat compile --no-typechain",
    "deploy-token": "npx hardhat run --network localhost dist/src/deploy/deploy-token.js",
    "deploy-aa": "npx hardhat run --network localhost dist/src/deploy/deploy-aa.js",
    "deploy": "npm run compile && npm run deploy-token && npm run deploy-aa",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@types/node": "^20.2.5",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0",
    "tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "ethers": "^5.7.2",
    "userop": "^0.2.0"
  }
}
